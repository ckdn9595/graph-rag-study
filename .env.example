# Claude Agent SDK는 로컬 Claude CLI를 사용하므로 API 키 불필요
# (Claude Pro/Team 구독 필요)

# MySQL Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name

# Agent Settings
DEFAULT_METHOD=yaml
MAX_VALIDATION_RETRIES=3
MAX_TURNS=100
